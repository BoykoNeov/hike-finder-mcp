# Core runtime
requests>=2.31
mcp>=1.2          # official MCP Python SDK (server.py)

# Optional: only needed for the local DEM elevation backend
# rasterio>=1.3   # heavy, GDAL-backed; uncomment to enable mode=local

# Dev / test
pytest>=8.0
